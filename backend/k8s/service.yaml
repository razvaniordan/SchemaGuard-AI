apiVersion: v1
kind: Service
metadata:
  name: schemeguard-ai-backend
  labels:
    app: schemeguard-ai-backend
spec:
  type: ClusterIP
  selector:
    app: schemeguard-ai-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
